{
  "name": "game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"tsc -p tsconfig.json -w\" \"nodemon --watch dist --exec node dist/server.js\"",
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node dist/server.js"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/websocket": "^10.0.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/ws": "^8.5.12",
    "typescript": "^5.5.3",
    "tsx": "^4.16.2"
  }
}
